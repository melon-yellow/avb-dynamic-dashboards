
##########################################################################################################################

version: "3"

services:

##########################################################################################################################

  dyn:
    image: php:8-apache
    container_name: dyn-dash
    build: ./dyn
    env_file: .env
    volumes:
      - ./dyn/:/var/www/html/
    ports:
      - "${PHP_APACHE_PORT}:80"
    depends_on:
      - db
    restart: always

##########################################################################################################################
